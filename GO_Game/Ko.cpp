#include "Go.h"
#include "Ko.h"

// Pas terminée
void IsKo(int type, int y, int x, goban g){
    int enleveUp;
    int enleveRi;
    int enleveDw;
    int enleveLe;
    int enleveTotal;
    int koflag;
    
    if(type != g.board[y - 1][x]&&type != board[y][x + 1]&&type = board[y + 1][x]&&type = board[y][x - 1]){
        
    }
}
